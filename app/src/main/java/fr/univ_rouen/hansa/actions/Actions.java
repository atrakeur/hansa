package fr.univ_rouen.hansa.actions;

public enum Actions {
    liberSophia,
    increasePower,
    movePawnRtoGB,
    keepKontor,
    bursa,
    movePawnRtoS, //Move Supply (accessible) to Stock (unaccessible)
    endRound,
    playBonus,
    keepRoute,
    replaceMovedPawn
}
